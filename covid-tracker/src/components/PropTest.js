import React from "react";

function PropTest(props) {
    console.log(props.infoDisplay)
    return(
        <select>

             <option> </option>
            
        </select>
    )
}

export default PropTest;